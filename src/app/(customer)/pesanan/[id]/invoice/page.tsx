import { FeaturePlaceholder } from '@/components/shared/FeaturePlaceholder';
export default function Page(){return <FeaturePlaceholder title='Invoice' description='Total berat × snapshot harga/kg + snapshot shipping.'/>}
